document.documentElement.classList.add('js');
const header=document.querySelector('[data-header]'),toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('#site-nav'),reduceMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;
function setMenu(open,returnFocus=false){if(!header||!toggle||!nav)return;header.classList.toggle('is-open',open);document.body.classList.toggle('nav-open',open);toggle.setAttribute('aria-expanded',String(open));toggle.querySelector('span').textContent=open?'Close':'Menu';if(open)nav.querySelector('a')?.focus();if(!open&&returnFocus)toggle.focus()}
toggle?.addEventListener('click',()=>setMenu(toggle.getAttribute('aria-expanded')!=='true'));nav?.addEventListener('click',e=>{if(e.target.closest('a'))setMenu(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&toggle?.getAttribute('aria-expanded')==='true')setMenu(false,true)});
const onScroll=()=>header?.classList.toggle('is-scrolled',scrollY>32);onScroll();addEventListener('scroll',onScroll,{passive:true});
document.querySelectorAll('[data-year]').forEach(n=>{n.textContent=new Date().getFullYear()});
const reveals=document.querySelectorAll('[data-reveal]');if(reduceMotion||!('IntersectionObserver'in window)){reveals.forEach(n=>n.classList.add('is-visible'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');observer.unobserve(entry.target)}),{threshold:.08,rootMargin:'0px 0px -8% 0px'});reveals.forEach(n=>observer.observe(n))}
document.querySelectorAll('[data-video-id]').forEach(slot=>{const id=slot.dataset.videoId?.trim();if(!id)return;const frame=document.createElement('iframe');frame.src=`https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}`;frame.title=slot.dataset.videoTitle||'I Dream Studio film';frame.loading='lazy';frame.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share';frame.allowFullscreen=true;slot.replaceChildren(frame)});
